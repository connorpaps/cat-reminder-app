node_modules/
out/
release/
.vite/
coverage/
*.db
*.db-shm
*.db-wal
.env
.env.*
!.env.example
.playwright-cli/
# Auto-memory local watcher log (raw per-save events; the commit log is tracked)
docs/activity-watch.log
# Machine marker (hostname of last machine used — local state, not project knowledge)
docs/.last-machine
